import { EyeColorDirective } from './eye-color.directive';

describe('EyeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new EyeColorDirective();
    expect(directive).toBeTruthy();
  });
});
